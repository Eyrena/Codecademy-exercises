#include <vector>
#include <string>
void greet();
void end_game(std::string codeword, std::string answer);
void display_misses(int misses);
void display_status(std::string incorrect, std::string answer);

// Declare functions
